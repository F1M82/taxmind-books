"""HTTP API package — routes and dependencies.

Routes live under `app.api.v1.*` (versioned). This module's `deps`
submodule defines the FastAPI dependencies that resolve the active
user, company, and database session.
"""
