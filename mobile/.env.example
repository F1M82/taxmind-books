# TaxMind Books — Mobile env example
#
# Configure the API base URL by replacing app.json `extra.API_BASE_URL`
# at build time (e.g., via `app.config.js`) — Expo doesn't read .env
# at build for native bundles. This file is documentation only.

API_BASE_URL=http://localhost:8000
