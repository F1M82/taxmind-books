# Phase 0 §7.5a live validation — P0.46d lifecycle (Sales voucher path)

**Generated:** 2026-05-18T07:45:47Z
**Git SHA:** `de8b6d8e91b2cd05747ffec8604d37166527b3b6` (`de8b6d8`)
**Branch:** `main`
**Working tree:** dirty — intentional v1.3 doc staging (`docs/AMENDMENTS_v1.3.md`,
`docs/CONNECTOR_ENROLLMENT.md`, mobile/eas.json, etc.) NOT committed; these are
the Phase 0.5 staging set called out in `memory/phase_0_validation_state.md`.
**Scope:** §7.5a only. §7.5b deferred — connector + live Tally not exercised
this session.

---

## 1. Preconditions

| Item | Expected | Observed |
|---|---|---|
| Tally main process | offline (port 9000 not listening) | `Test-NetConnection :9000` → False |
| Postgres | up | port 5432 listening, container `taxmind-postgres` healthy 45h |
| Redis | up | port 6379 listening |
| Backend | started this session | uvicorn pid 35680 on :8000, `/docs` → 200 |
| Backend code | P0.46d shipped on `main` | de8b6d8, contains commits a66c9c3 (backend lifecycle) + 2fe6dcf (mobile badge) |
| DB schema | alembic head `0010` | **Started at `0009` — migration applied this session (see §2).** |

### 1.1 Bring-up actions

- `.env` created at repo root from `.env.example`; secrets regenerated with
  `secrets.token_urlsafe(48)`; copy mirrored to `backend/.env` so
  pydantic-settings finds it under uvicorn's CWD.
- pre-migration `pg_dump taxmind_books > validation/backup_pre_0010.sql`
  (1,513 lines, 201,765 bytes).
- `alembic upgrade head` (0009 → 0010, P0.46d). Verified post-migration:
  - `alembic_version = 0010`
  - `voucher_status` enum now includes `pending_tally_post`
  - `vouchers.tally_post_queued_at` column exists
  - `idx_vouchers_unposted_to_tally` repointed to
    `WHERE status = 'pending_tally_post'`
- Existing test user `test@taxmindbooks.dev` password reset via
  `bcrypt(hash_password)` direct UPDATE so the live API session can
  authenticate as the owner of the company holding the §7.5 synced ledgers.
- Login via `POST /api/v1/auth/login` → 200, access_token issued.

## 2. Test fixture

| Field | Value |
|---|---|
| User | `test@taxmindbooks.dev` (id `79910b81-2a28-4fa4-8745-ee9d468a65bc`) |
| Company | `Taxmind Books` (id `58f01ad7-88ad-4eb5-bf49-664a665185f7`) |
| Xyz Ltd ledger | `8d919437-2a12-43ba-bb80-ac30459ae4b7` (group `Sundry Debtors`, from §7.5 sync_masters PASS 2026-05-16) |
| Sales ledger | `eb82b7bd-e1e0-412c-9a75-52505228e1df` — **created this session** via `POST /api/v1/ledgers/` (group `Sales Accounts`, balance_type Cr), idem-key `v75a-sales-ledger-2026-05-18` |

The Sales ledger is the precondition called out in
`docs/validation_followup.md` so the test company has an income leg — without
it, a Sales voucher is impossible and the §7.5a P&L follow-up cannot close.

---

## 3. §7.5a results

### 3.1 POST voucher while Tally stopped → 201 with `status="pending_tally_post"`, `tally_post_queued_at` populated, `tally_posted_at` null; `voucher.created` audit row written — **PASS**

- Request: `POST /api/v1/vouchers/` with
  `Idempotency-Key: v75a-sales-voucher-2026-05-18-py`,
  `X-Company-ID: 58f01ad7-…`
- Body: Sales voucher, date 2026-05-18, total 100.00,
  entries Xyz Ltd Dr 100 / Sales Cr 100.
- Response: **201**.
  ```
  id:                      2d555535-7b3f-471f-a7aa-34a13ea42060
  status:                  pending_tally_post
  tally_post_queued_at:    2026-05-18T07:43:01.773879Z
  tally_posted_at:         null
  tally_post_attempts:     0
  tally_last_error:        null
  source:                  manual
  created_by:              79910b81-… (test@taxmindbooks.dev)
  ```
- Audit log (DB query):
  ```
  voucher.created  entity=2d555535-… user=79910b81-… source=api
                   request_id=7e147a05-3728-4c0a-bf7d-666b224b6794
                   at=2026-05-18T07:43:01.771380Z
  ```
- All four sub-assertions of the checkbox satisfied:
  - 201 status ✓
  - `status="pending_tally_post"` ✓
  - `tally_post_queued_at` populated ✓
  - `tally_posted_at` null ✓
  - `voucher.created` audit row written ✓

#### 3.1.a Duplicate cleanup

The first POST attempt (`Idempotency-Key: v75a-sales-voucher-2026-05-18`)
appeared to fail in the PowerShell session with HTTP 400
`"There was an error parsing the body"` (caused by a UTF-16/BOM encoding
on the here-string body). Server-side it actually succeeded —
voucher `73db3ec5-d082-49e7-ab04-24cb9a39f589` was created and audited at
2026-05-18T07:41:01Z. To keep the report deltas clean, that duplicate was
cancelled via
`POST /api/v1/vouchers/73db3ec5-…/cancel` (200, status now `cancelled`).
The cancelled row appears in §3.2 as
`cancelled_vouchers_excluded_count: 1`.

### 3.2 Trial balance / P&L include the queued voucher (book-truthful) — **PASS**

`GET /api/v1/reports/trial-balance?as_of_date=2026-05-18`:

| Ledger | Group | Period Dr | Period Cr | Closing |
|---|---|---|---|---|
| Cash | Cash-in-Hand | 0.00 | 1000.00 | 1000.00 Cr |
| Sales | Sales Accounts | 0.00 | **100.00** | **100.00 Cr** |
| ABC LTD | Sundry Creditors | 1000.00 | 0.00 | 1000.00 Dr |
| Xyz Ltd | Sundry Debtors | **100.00** | 0.00 | **100.00 Dr** |

- `totals.total_dr = total_cr = 1100.00`, `in_balance = true`
- `exclusions.cancelled_vouchers_excluded_count = 1` (the §3.1.a duplicate)
- `exclusions.optional_vouchers_excluded_count = 0`
- The pre-existing Cash/ABC LTD `1000.00` pair is a legacy Payment voucher
  (`7a7f4f81-…`, status `posted`, `tally_posted_at` null) carried over
  from 2026-05-16 §7.5 walk-through. Status was set by pre-P0.46d code
  (the very conflation P0.46d split); kept untouched as it doesn't affect
  this run's deltas.

`GET /api/v1/reports/profit-loss?from_date=2026-04-01&to_date=2026-05-18`:

```
income.ledgers[0]:  Sales  100.00
income.total:       100.00
expense.total:      0.00
net:                profit 100.00
```

- The queued Sales voucher (status `pending_tally_post`,
  `tally_posted_at` null) is included in `income.total` — proves the
  `Voucher.status.in_([posted, pending_tally_post])` filter in
  `app/services/reporting/profit_loss.py` works end-to-end against live
  data, not just the unit-level regression
  `test_profit_loss_includes_pending_tally_post_vouchers`.
- **Closes the §7.5a P&L follow-up in `docs/validation_followup.md`.**

### 3.3 Mobile voucher list shows "Queued for Tally" badge — **PASS (data contract verified; UI render deferred to §7.6)**

`GET /api/v1/vouchers/?limit=5` returned for voucher `2d555535-…`:
```
status:                  pending_tally_post
tally_posted_at:         null
tally_post_queued_at:    2026-05-18T07:43:01.773879Z
tally_last_error:        null
```

Tracing through `mobile/src/screens/vouchers/VoucherListScreen.tsx`:

1. `isTallyRejected(v)` → false (`tally_last_error === null`).
2. `tallyTagLabel(v)`: skips cancelled/rejected/posted/rejected branches,
   matches `v.status === "pending_tally_post"` → returns
   **`"Queued for Tally"`** (line 40).
3. `tallyTagStyle(v)` → `styles.tallyQueued` (line 50).

Badge data contract verified end-to-end; mobile suite is green
(35 tests / 10 suites at f0c5fc0) including the screen unit test from
commit `2fe6dcf`. Full Expo render against a live device is not exercised
this session — it falls under §7.6 (mobile end-to-end), which is the
appropriate place for it.

---

## 4. §7.5b — not exercised this session

§7.5b requires the connector running on a Windows VM with TallyPrime up,
which contradicts the §7.5a precondition (Tally stopped). Re-validating
§7.5b is the next session's work; the checklist items
(lines 333–340, 342, 344 of `docs/VALIDATION_REPORT.md`) remain open.

`memory/phase_0_validation_state.md` updated to reflect §7.5a PASS;
§7.5b stays as the next-session item.

---

## 5. Artefacts produced this session

| Path | Purpose |
|---|---|
| `.env`, `backend/.env` | local dev config (gitignored) |
| `validation/backup_pre_0010.sql` | DB dump pre-migration (1,513 lines) |
| `validation/uvicorn_stdout.log`, `validation/uvicorn_stderr.log` | backend runtime logs |
| `validation/voucher_body.json` | JSON body fixture (transient) |
| `validation/phase_0_20260518_074547.md` | **this file** |

## 6. Database state at end of session

```
alembic_version          0010
vouchers (this company)  pending_tally_post=1, posted=1 (legacy), cancelled=1 (dup)
ledgers (this company)   6 (5 synced + 1 Sales created here)
audit rows added         voucher.created (2d555535), voucher.cancelled (73db3ec5),
                         ledger.created (eb82b7bd)
```

No commits, no push. Working tree v1.3 doc deltas untouched per the
session brief.
